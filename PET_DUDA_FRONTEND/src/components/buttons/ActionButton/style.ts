import styled from 'styled-components'

export const ActionButtonStyled = styled.button`
    width: fit-content;
    white-space: nowrap;
    padding: 12px;
    border-radius: 8px;
    border: none;

    color: white;

    font-size: 16px;
    font-weight: bold;

    cursor: pointer;

    transition: 0.2s;

`